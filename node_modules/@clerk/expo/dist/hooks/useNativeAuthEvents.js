"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useNativeAuthEvents_exports = {};
__export(useNativeAuthEvents_exports, {
  useNativeAuthEvents: () => useNativeAuthEvents
});
module.exports = __toCommonJS(useNativeAuthEvents_exports);
var import_react = require("react");
var import_react_native = require("react-native");
var import_native_module = require("../utils/native-module");
function useNativeAuthEvents() {
  const [nativeAuthState, setNativeAuthState] = (0, import_react.useState)(null);
  (0, import_react.useEffect)(() => {
    if (!import_native_module.isNativeSupported || !import_native_module.ClerkExpoModule) {
      return;
    }
    let subscription = null;
    try {
      const eventEmitter = new import_react_native.NativeEventEmitter(import_native_module.ClerkExpoModule);
      subscription = eventEmitter.addListener("onAuthStateChange", (event) => {
        if (false) {
          console.log("[useNativeAuthEvents] onAuthStateChange:", JSON.stringify(event));
        }
        setNativeAuthState(event);
      });
    } catch (error) {
      if (false) {
        console.error("[useNativeAuthEvents] Failed to set up event listener:", error);
      }
    }
    return () => {
      subscription == null ? void 0 : subscription.remove();
    };
  }, []);
  return {
    nativeAuthState,
    isSupported: import_native_module.isNativeSupported && !!import_native_module.ClerkExpoModule
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useNativeAuthEvents
});
//# sourceMappingURL=useNativeAuthEvents.js.map